<br>
<%= link_to "New Trip", new_trip_path, class: 'button' %>
<br>
<%# <div class="trip-tabs" data-controller="trip-tabs">
  <button id="upcoming" class="recommendation-button">Upcoming</button>
  <button id="past" class="recommendation-button">Past</button> %>
</div>
<div class="parent">
<div class="favourites">
  <% @trips.each do |trip| %>
    <div class="favourite-card">
      <div class="favourite-text">
        <h5><%= trip.trip_name %></h5>
        <h6>Departing: <%= trip.start_date.strftime('%d-%m-%Y') %></h6>
      </div>
      <div class="add-button">
        <%= link_to "View Trip", trip_path(trip.id), class: 'button' %>
      </div>
    </div>
  <% end %>
</div>
</div>
